package voldaran.com.Upright;

import android.util.Log;

public class MapClass {
	
	
	
	
}
	
	/*int tileSize = 16;
	int ROWS = 200;
	int COLS = 200;
	int x = 0;
	private TileClass[][] aBoard = new TileClass[ROWS][COLS];
	
	
	public TileClass[][] getBoard () {
		return aBoard;
	}
	
	public void Initialize () {
		for (int row = 0; row < 200; row++) {
			for (int col = 0; col < 200; col++) {
				aBoard[row][col] = new TileClass();
				aBoard[row][col].tileSize = tileSize;
				aBoard[row][col].beginX = 16 * col;
				aBoard[row][col].beginY = 16 * row;
				aBoard[row][col].stringName = "" + row + "," + col;
				
				x += 1;
				Log.d("GSTA", "Building Level " + x + " out of " + ROWS * COLS);
			}
		}
		
		
		Log.d("GSTA", "here");
		for (int row = 0; row < 200; row++) {
			for (int col = 0; col < 200; col++) {
				Log.d("GSTA", ":" + row + "," + col + ": " + aBoard[row][col].beginX + "," + aBoard[row][col].beginY); 
			}
		}
			
	}
	
	
	public boolean ifExist(int row, int column) {
		
		
		return false;
	}
	
	public void setTileSize (int size) {
		tileSize = size;
	}
	
	public void addTile(int column, int row) {
		
		
	}
	
	
	public class TileClass {
		String stringName;
		int beginX, beginY, tileSize;
		
		
		
	}
}*/
